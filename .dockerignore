# Keep Git objects and refs so Go's default VCS stamping records vcs.revision and vcs.time.
# Exclude checkout-local configuration, hooks, and logs because they are not build inputs and may contain credentials.
.git/config
.git/config.worktree
.git/hooks
.git/logs
web/node_modules
web/dist
